{
  "name": "macgyver-ui-packages",
  "private": true,
  "scripts": {
    "storybook": "start-storybook -p 9002 -c .storybook",
    "flow": "flow",
    "boostrap": "lerna bootstrap --hoist && lerna run flow-typed -- install",
    "clean": "lerna clean && lerna run clean -- -s"
  },
  "dependencies": {
    "@storybook/addon-info": "^3.2.18",
    "@storybook/addon-knobs": "^3.2.18",
    "@storybook/addon-options": "^3.3.3",
    "@storybook/react": "^3.2.17",
    "babel-core": "^6.26.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.61.0",
    "lerna": "^2.8.0"
  }
}
